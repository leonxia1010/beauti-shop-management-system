# Environment Configuration Example
# Copy this file to .env and update values for local development
#
# ⚠️ SECURITY WARNING ⚠️
# This file contains DEFAULT DEVELOPMENT credentials.
# NEVER use these values in production!
# ALWAYS change all passwords, secrets, and keys before deploying.
#
# Required changes for production:
# - All passwords must be strong and unique
# - JWT secrets must be cryptographically secure random strings
# - AWS credentials must be environment-specific
# - Use AWS Secrets Manager for sensitive values in production

# Application Environment
NODE_ENV=development

# Database Configuration (PostgreSQL)
# Aligns with AWS RDS configuration in production
# ⚠️ Change 'postgres' password before production use
DB_HOST=localhost
DB_PORT=5433
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE=beauty_shop
DATABASE_URL=postgresql://postgres:postgres@localhost:5433/beauty_shop

# Redis Configuration
# Aligns with AWS ElastiCache configuration in production
# ⚠️ Change 'redis_password' before production use
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=redis_password
REDIS_URL=redis://:redis_password@localhost:6379

# JWT Authentication
# Use AWS Secrets Manager in production
# ⚠️ CRITICAL: Generate cryptographically secure random secrets for production
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRATION=7d
REFRESH_TOKEN_SECRET=your-refresh-token-secret
REFRESH_TOKEN_EXPIRATION=30d

# AWS Configuration (Production)
# These should be managed via AWS Secrets Manager in production
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your-access-key-id
AWS_SECRET_ACCESS_KEY=your-secret-access-key

# AWS S3 Storage
S3_BUCKET_NAME=beauty-shop-assets
S3_REGION=us-east-1

# Email Service (Amazon SES)
SES_REGION=us-east-1
FROM_EMAIL=noreply@beauty-shop.com

# Application URLs
API_BASE_URL=http://localhost:3000
FRONTEND_URL=http://localhost:4200
MOBILE_APP_URL=http://localhost:19006

# Admin Web Environment Variables
VITE_API_URL=http://localhost:3000
VITE_APP_NAME=Beauty Shop Management

# Mobile App Environment Variables
EXPO_PUBLIC_API_URL=http://localhost:3000

# Logging Configuration
LOG_LEVEL=debug
LOG_FORMAT=pretty

# Queue Configuration (BullMQ with Redis)
QUEUE_REDIS_HOST=localhost
QUEUE_REDIS_PORT=6379
QUEUE_REDIS_PASSWORD=redis_password

# File Upload Configuration
MAX_FILE_SIZE=10485760  # 10MB in bytes
ALLOWED_FILE_TYPES=jpg,jpeg,png,gif,pdf

# Security Configuration
BCRYPT_SALT_ROUNDS=10
CORS_ORIGINS=http://localhost:4200,http://localhost:19006

# Development Tools
ENABLE_SWAGGER=true
SWAGGER_PATH=api/docs

# Health Check Configuration
HEALTH_CHECK_DATABASE=true
HEALTH_CHECK_REDIS=true